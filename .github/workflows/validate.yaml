name: HACS Validation

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions: {}

jobs:
  hacs:
    runs-on: "ubuntu-latest"
    steps:
      - name: Validate HACS
        uses: "hacs/action@main"
        with:
          category: "plugin"